# Project Summary

`atomsDemoV1` 是一个面向 AI 原型开发的可运行应用，帮助用户通过自然语言创建、生成、预览和管理应用项目。项目支持匿名与登录用户数据隔离、认证恢复、项目版本管理、代码与预览查看、生成失败恢复、版本回滚，以及演示项目只读保护。当前项目中可能被预览系统调用的两份 `start_app_v2.sh` 均已修复为 Unix LF 换行并补齐执行权限，能够通过 Bash 解析和参数检查，避免 CRLF 导致预览进程异常退出。

# Project Module Description

- **AI 生成工作流**：接收用户需求，调用 AIHub 生成项目内容，展示生成步骤、状态和错误信息。
- **匿名与登录数据隔离**：匿名用户通过服务端签名 Cookie 关联数据；登录后不合并匿名数据。
- **认证与用户管理**：支持认证恢复、登录回调、退出登录及受保护操作，未登录用户仍可使用匿名工作流。
- **项目与版本管理**：支持创建、查询、删除项目，查看版本历史并切换版本。
- **版本回滚**：通过创建新版本实现回滚，避免覆盖历史版本，并同步项目当前状态。
- **演示项目保护**：无归属演示项目可全员查看，前后端均禁止删除、生成等写操作。
- **代码与结果预览**：保持 Preview、源代码和版本内容一致，支持生成结果查看。
- **生成稳定性与错误处理**：处理鉴权、限流、超时、连接错误和 5xx 错误，支持重试、阶段超时和心跳等待。
- **历史上下文控制**：限制多轮历史上下文长度，单条历史内容不超过 400 字。
- **数据持久化与 API**：使用 SQLAlchemy/Alembic 管理用户、项目、版本、消息和生成步骤，并提供 REST API。
- **应用启动与验证**：同步修复项目内及上传源中的同名启动脚本，统一使用 Unix LF 换行并具备可执行权限。

# Directory Tree

```text
app/
├── .mgx/                         # 项目协作配置
├── docs/
│   └── 优化实施记录.md           # 数据隔离与生成链路优化实施记录
├── start_app_v2.sh               # 应用一键启动脚本
├── backend/
│   ├── main.py                   # FastAPI 应用入口
│   ├── lambda_handler.py         # 无服务器部署入口
│   ├── requirements.txt          # 后端依赖
│   ├── requirements.default      # 默认依赖模板
│   ├── alembic.ini               # 数据库迁移配置
│   ├── alembic/                  # Alembic 环境与迁移脚本
│   ├── core/                     # 配置、数据库、认证、加密和遥测
│   ├── dependencies/
│   │   └── owner.py              # 项目归属与访问控制依赖
│   ├── models/                   # SQLAlchemy 数据模型
│   ├── schemas/                  # API 请求/响应结构
│   ├── routers/                  # API 路由模块
│   ├── services/                 # AI、项目、版本和存储服务
│   ├── data_models/              # 数据结构定义
│   ├── mock_data/                # 模拟数据
│   ├── skills_docs/              # AI、存储和 SDK 使用说明
│   ├── middlewares/              # 后端中间件
│   ├── utils/                    # 通用工具
│   └── tests/                    # 后端测试
└── frontend/
    ├── package.json              # 前端依赖与脚本
    ├── vite.config.ts            # Vite 配置
    ├── tailwind.config.ts        # Tailwind 配置
    ├── components.json           # UI 组件配置
    ├── site.config.json          # 站点配置
    ├── template_config.json      # 模板配置
    ├── public/                   # 静态资源
    ├── prerender/                # 博客预渲染与站点地图脚本
    └── src/
        ├── main.tsx              # 前端入口
        ├── App.tsx               # 根组件、路由与认证 Provider
        ├── pages/                # 工作区、认证和博客页面
        ├── components/           # 项目、对话、预览、代码和版本组件
        ├── components/ui/        # 通用 UI 组件
        ├── contexts/             # 认证上下文
        ├── hooks/                # 可复用 React Hooks
        ├── lib/                  # API、认证、Atoms 和工具函数
        ├── api/                  # 设置接口封装
        └── *.css                 # 全局与应用样式
```

# File Description Inventory

| 文件/目录 | 说明 |
|---|---|
| `app/start_app_v2.sh` | 一键启动脚本，使用 Unix LF 换行并具备执行权限 |
| `backend/routers/atoms.py` | Atoms 能力及项目接口，处理归属、匿名访问和演示项目只读策略 |
| `backend/dependencies/owner.py` | 根据登录身份或匿名 Cookie 解析项目归属并执行访问控制 |
| `backend/services/aihub.py` | AIHub 调用、错误分类、重试和超时处理 |
| `backend/services/pipeline.py` | AI 生成流水线、阶段状态、失败恢复和历史上下文控制 |
| `backend/services/html_extract.py` | HTML 完整性提取与校验 |
| `backend/services/prompts.py` | 生成提示词和上下文构造 |
| `backend/tests/test_pipeline_errors.py` | AI 生成错误分类与恢复逻辑测试 |
| `backend/tests/` | 后端功能、上下文截断及生成链路回归测试 |
| `app/docs/优化实施记录.md` | 数据隔离、生成稳定性、回滚和验收记录 |
| `frontend/src/App.tsx` | 应用路由、认证 Provider 与全局状态入口 |
| `frontend/src/contexts/AuthContext.tsx` | 登录、退出、认证恢复和三态认证状态 |
| `frontend/src/pages/Index.tsx` | 工作区页面及项目、版本、预览状态协调 |
| `frontend/src/components/ProjectList.tsx` | 项目列表、演示项目提示和操作限制 |
| `frontend/src/components/PromptInput.tsx` | 需求输入、生成触发及只读状态反馈 |
| `frontend/src/components/VersionSwitcher.tsx` | 版本查看、切换和回滚入口 |
| `frontend/src/lib/atoms.ts` | Atoms API 客户端及生成、取消、恢复接口 |
| `frontend/src/lib/constants.ts` | 前端状态、提示和业务常量 |
| `frontend/src/lib/api.ts` | 通用 API 请求封装 |
| `frontend/src/components/PreviewPane.tsx` | 生成结果预览 |
| `frontend/src/components/CodeViewer.tsx` | 生成代码查看 |
| `frontend/src/components/AgentSteps.tsx` | AI 生成步骤展示 |

# Technology Stack

- **前端**：React、TypeScript、Vite、Tailwind CSS
- **UI**：Radix UI、shadcn 风格组件、Lucide 图标
- **后端**：Python、FastAPI、Pydantic
- **数据层**：SQLAlchemy、Alembic
- **AI 能力**：Atoms Cloud、AIHub
- **认证与身份**：平台 Web SDK、JWT 配置、服务端签名 Cookie
- **存储**：对象存储服务封装
- **质量保障**：ESLint、TypeScript 构建检查、Python 语法检查、pytest、Bash 语法检查
- **部署形态**：前端生产静态构建、FastAPI 服务、Lambda 入口、Bash 一键启动

# Usage

## 安装后端依赖

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## 初始化数据库

配置数据库、认证、JWT 和 AI 服务相关环境变量后执行：

```bash
cd app/backend
alembic upgrade head
```

## 启动后端

```bash
cd app/backend
uvicorn main:app --reload
```

## 安装前端依赖

```bash
cd app/frontend
npm install
```

## 前端开发运行

```bash
cd app/frontend
npm run dev
```

## 静态检查与生产构建

```bash
cd app/frontend
npm run lint
npm run build
```

## 后端检查与测试

```bash
cd app/backend
python -m compileall .
pytest
```

## 验证启动脚本

```bash
cd app
bash -n start_app_v2.sh
bash start_app_v2.sh --help
```

启动脚本需要在 Unix 环境中执行，并保持 Unix LF 换行格式及可执行权限。上传源目录中的同名脚本也应保持相同格式，避免迁移后重新引入 CRLF 问题。

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

## 验证重点

- 验证匿名会话之间及匿名与登录用户之间的数据隔离。
- 验证登录回调、认证恢复和退出登录。
- 验证演示项目只能查看，删除、生成等写操作被前后端同时阻止。
- 验证生成失败分类、重试、超时、取消和恢复流程。
- 验证版本回滚通过创建新版本完成，并保持项目、Preview 和源代码一致。
- 验证项目内及上传源中的启动脚本可通过 Bash 语法和参数解析检查。
- 发布前在 App Viewer 中人工检查双匿名会话、登录回调和真实模型生成，再执行发布。
