# Project Summary

`atomsDemoV1` 是一个可运行、可预览和可发布的 AI 原型应用。项目提供基于对话的项目生成流程，支持项目管理、版本切换、生成步骤展示、代码查看、预览面板、用户认证及后端 AI 服务接入。当前原型已迁移至 `app` 目录，后端已激活，前端已通过静态检查和生产构建，并已启动预览验证。

# Project Module Description

- **AI 生成工作流**：通过对话输入需求，调用 AIHub 服务生成项目内容，并展示生成步骤、消息和错误状态。
- **项目与版本管理**：创建和管理项目，查看项目版本并在不同版本之间切换。
- **代码与结果预览**：使用代码查看器展示生成内容，通过预览面板查看应用效果。
- **认证与用户管理**：支持登录回调、退出登录、受保护路由和用户身份依赖注入。
- **后端 API**：提供健康检查、认证、项目、版本、消息、生成步骤、设置、存储和原子能力等接口。
- **数据持久化**：使用 SQLAlchemy 模型和 Alembic 迁移管理认证、项目、版本、消息及生成步骤数据。
- **存储与扩展能力**：封装对象存储、HTML 提取、支付、提示词、流水线及 Web SDK 等服务。
- **博客内容**：前端包含博客索引、文章页面、Markdown 渲染及静态路由/站点地图预渲染能力。
- **可观测性**：后端提供 HTTP、SQLAlchemy、请求摘要和耗时等遥测中间件。

# Directory Tree

```text
app/
├── .mgx/                         # 项目协作配置
├── start_app_v2.sh               # 应用启动脚本
├── backend/
│   ├── main.py                   # FastAPI 应用入口
│   ├── lambda_handler.py        # 无服务器部署入口
│   ├── requirements.txt          # 后端依赖
│   ├── requirements.default      # 默认依赖模板
│   ├── alembic.ini               # 数据库迁移配置
│   ├── alembic/                  # Alembic 环境与迁移脚本
│   ├── core/                     # 配置、数据库、认证、枚举、加密和遥测
│   ├── dependencies/             # 认证与数据库依赖注入
│   ├── models/                   # SQLAlchemy 数据模型
│   ├── schemas/                  # API 请求/响应结构
│   ├── routers/                  # API 路由模块
│   ├── services/                 # 业务服务和第三方能力封装
│   ├── data_models/              # 数据结构定义
│   ├── mock_data/                # 模拟数据
│   ├── skills_docs/              # AI、存储和 SDK 使用说明
│   ├── middlewares/              # 后端中间件
│   ├── utils/                    # 日志等通用工具
│   └── tests/                    # 后端测试
└── frontend/
    ├── package.json              # 前端依赖与脚本
    ├── vite.config.ts            # Vite 配置
    ├── tailwind.config.ts        # Tailwind 配置
    ├── components.json           # UI 组件配置
    ├── site.config.json          # 站点配置
    ├── template_config.json      # 模板配置
    ├── public/                   # 图标和爬虫规则等静态资源
    ├── prerender/                # 博客预渲染与站点地图脚本
    └── src/
        ├── main.tsx              # 前端入口
        ├── App.tsx               # 根组件与路由
        ├── pages/                # 主页面、认证页面和博客页面
        ├── components/           # 项目列表、对话、预览、代码和生成步骤组件
        ├── components/ui/        # 通用 UI 组件库
        ├── contexts/              # 认证上下文
        ├── hooks/                 # 可复用 React Hooks
        ├── lib/                   # API、认证、配置、博客和工具函数
        ├── api/                   # 设置接口封装
        └── *.css                 # 全局与应用样式
```

# File Description Inventory

| 文件/目录 | 说明 |
|---|---|
| `backend/main.py` | 创建 FastAPI 应用并注册路由、中间件 |
| `backend/core/auth.py` | 认证核心逻辑 |
| `backend/core/config.py` | 环境变量与应用配置 |
| `backend/core/database.py` | 数据库连接和会话管理 |
| `backend/core/mask_crypto.py` | 敏感信息掩码及加密处理 |
| `backend/core/telemetry/` | 请求、数据库和性能遥测 |
| `backend/models/` | 用户、项目、版本、消息和生成步骤模型 |
| `backend/routers/` | 按业务拆分的 REST API |
| `backend/services/aihub.py` | AIHub 调用及生成服务 |
| `backend/services/pipeline.py` | AI 生成流水线编排 |
| `backend/services/storage.py` | 对象存储服务 |
| `backend/services/projects.py` | 项目业务逻辑 |
| `backend/services/versions.py` | 版本业务逻辑 |
| `backend/services/messages.py` | 消息业务逻辑 |
| `backend/services/generation_steps.py` | 生成步骤业务逻辑 |
| `backend/services/auth.py` | 认证服务 |
| `backend/alembic/versions/` | 数据库迁移版本 |
| `backend/tests/` | HTML 提取和上下文记忆测试 |
| `frontend/src/pages/Index.tsx` | 原型主工作区 |
| `frontend/src/components/ConversationPanel.tsx` | 对话交互面板 |
| `frontend/src/components/PromptInput.tsx` | 用户需求输入组件 |
| `frontend/src/components/AgentSteps.tsx` | AI 生成步骤展示 |
| `frontend/src/components/CodeViewer.tsx` | 生成代码查看 |
| `frontend/src/components/PreviewPane.tsx` | 应用预览区域 |
| `frontend/src/components/ProjectList.tsx` | 项目列表 |
| `frontend/src/components/VersionSwitcher.tsx` | 版本切换 |
| `frontend/src/contexts/AuthContext.tsx` | 全局认证状态 |
| `frontend/src/lib/api.ts` | API 请求封装 |
| `frontend/src/lib/auth.ts` | 前端认证封装 |
| `frontend/src/components/ui/` | 基于 Radix/shadcn 风格的 UI 组件 |
| `frontend/prerender/` | 博客页面预渲染和站点地图生成 |

# Technology Stack

- **前端**：React、TypeScript、Vite、Tailwind CSS
- **UI**：shadcn 风格组件、Radix UI、Lucide 图标
- **后端**：Python、FastAPI、Pydantic
- **数据层**：SQLAlchemy、Alembic
- **AI 能力**：Atoms Cloud / AIHub 服务
- **认证**：前后端认证回调与受保护路由
- **存储**：对象存储服务封装
- **质量保障**：ESLint、TypeScript 构建检查、Python 测试
- **部署形态**：前端生产静态构建、FastAPI 服务、无服务器 Lambda 入口

# Usage

## 安装后端依赖

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## 初始化数据库

根据运行环境配置数据库和认证相关环境变量后执行：

```bash
cd app/backend
alembic upgrade head
```

## 启动后端

```bash
cd app/backend
uvicorn main:app --reload
```

## 安装前端依赖

```bash
cd app/frontend
npm install
```

## 前端开发运行

```bash
cd app/frontend
npm run dev
```

## 静态检查与生产构建

```bash
cd app/frontend
npm run lint
npm run build
```

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

## 验证

- 访问前端主工作区并检查项目列表、对话输入、生成步骤、代码查看和预览面板。
- 验证认证回调、项目创建、版本切换及后端健康检查。
- 发布前确认环境变量、数据库迁移和对象存储配置已完成。
- 当前前端 lint 和生产构建已通过，预览实例已启动；正式发布需在 App Viewer 中确认页面后执行 Publish。
